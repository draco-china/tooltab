import { seo } from "@/lib/seo";
import { m } from "@/paraglide/messages.js";

export const scryptKeyDerivationHead = seo(
  m["tools.scryptKeyDerivation.name"],
  m["tools.scryptKeyDerivation.description"],
);
