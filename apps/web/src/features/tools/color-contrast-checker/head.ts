import { seo } from "@/lib/seo";
import { m } from "@/paraglide/messages.js";

export const colorContrastCheckerHead = seo(
  m["tools.colorContrastChecker.contrastName"],
  m["tools.colorContrastChecker.contrastDescription"],
);
