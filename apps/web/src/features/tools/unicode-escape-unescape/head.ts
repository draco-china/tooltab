import { seo } from "@/lib/seo";
import { m } from "@/paraglide/messages.js";

export const unicodeEscapeUnescapeHead = seo(
  m["shared.textCodecs.textcodecUnicodeName"],
  m["shared.textCodecs.textcodecUnicodeDescription"],
);
