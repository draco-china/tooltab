import { seo } from "@/lib/seo";
import { m } from "@/paraglide/messages.js";

export const currentNetworkTimeHead = seo(
  m["tools.currentNetworkTime.name"],
  m["tools.currentNetworkTime.description"],
);
