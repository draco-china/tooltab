import { seo } from "@/lib/seo";
import { m } from "@/paraglide/messages.js";

export const dockerRunToComposeConverterHead = seo(
  m["tools.dockerRunToComposeConverter.name"],
  m["tools.dockerRunToComposeConverter.description"],
);
