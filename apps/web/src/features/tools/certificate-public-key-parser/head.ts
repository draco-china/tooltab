import { seo } from "@/lib/seo";
import { m } from "@/paraglide/messages.js";

export const certificatePublicKeyParserHead = seo(
  m["tools.certificatePublicKeyParser.certParserTitle"],
  m["tools.certificatePublicKeyParser.certParserDescription"],
);
