import { seo } from "@/lib/seo";
import { m } from "@/paraglide/messages.js";

export const imageToAvifConverterHead = seo(
  m["common.formatavifname"],
  m["common.formatavifdescription"],
);

export const imageToIcoHead = seo(
  m["common.formaticoname"],
  m["common.formaticodescription"],
);
