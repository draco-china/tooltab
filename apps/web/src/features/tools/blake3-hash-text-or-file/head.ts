import { seo } from "@/lib/seo";
import { m } from "@/paraglide/messages.js";

export const blake3HashTextOrFileHead = seo(
  m["shared.blakeHash.blake3name"],
  m["shared.blakeHash.blake3description"],
);
