import { seo } from "@/lib/seo";
import { m } from "@/paraglide/messages.js";

export const pgpKeyGeneratorHead = seo(
  m["tools.pgpKeyGenerator.title"],
  m["tools.pgpKeyGenerator.description"],
);
