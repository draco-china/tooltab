# Public deployment origin used in metadata and AI/script examples.
VITE_BASE_URL=https://tooltab.example

# Optional server-only Cloudflare Zone Analytics credentials for popular tools.
# Never expose the token through VITE_ variables or client-side code.
CLOUDFLARE_ZONE_ID=
CLOUDFLARE_API_TOKEN=
