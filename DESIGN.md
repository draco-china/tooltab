---
version: 1
name: ToolTab Design System
implementation:
  library: "@heroui/react 3.2.4"
  tokenSource: "apps/web/src/styles/index.css"
---

# ToolTab 视觉与用户体验规范

## 产品与视觉

ToolTab 是本地处理优先的公共工具箱。`main` 提供浏览器工具；HTTP API 与 MCP 位于 `feature/api-mcp`。页面首先帮助用户找到工具、输入内容、完成操作并理解结果，说明围绕当前任务组织。

保留克制的 iOS 风格：系统字体、连续圆角、清晰层级、细边界和自然阴影。内容以开放布局呈现，Surface 用于有明确边界的工作区、表单、预览与弹层。避免密集嵌套卡片、整页玻璃效果、大面积装饰渐变、霓虹和启动动画。工具本身生成的渐变、图片与代码截图不受装饰限制。

用户指定的 Figma 节点优先于 Apple Human Interface Guidelines、HeroUI 能力和既有样式。参考用于层级、密度与交互语言，不复制品牌资源。没有新参考时沿用本契约，不自行引入另一套视觉系统。

本文件规定视觉、交互与无障碍要求。工程边界见 [AGENTS.md](AGENTS.md)，开发与部署见 [README.zh-CN.md](README.zh-CN.md)。规范本身不作为界面验收证据。

## 组件与 token

基础交互使用 HeroUI v3，优先组合已有 Button、TextField、Input、Autocomplete、Select、Tabs、Switch、Accordion、Disclosure、DropZone、FileTrigger 和 Modal。复用 [通用组件](apps/web/src/components/base)，不自行实现焦点管理、键盘选择或弹层状态机。TanStack Router Link 用于站内导航；语义 HTML 用于页面布局，Monaco 等专用编辑器保留其原生交互。

样式只有三个归属层级：

| 层级     | 来源                                       | 责任                                                     |
| -------- | ------------------------------------------ | -------------------------------------------------------- |
| 全局     | [index.css](apps/web/src/styles/index.css) | HeroUI 导入、语义 token、字体、容器、焦点与减少动态效果  |
| 配色     | [themes](apps/web/src/styles/themes)       | 每个文件一套亮暗配色，只覆盖颜色，不改变布局、圆角和密度 |
| 组件适配 | [heroui](apps/web/src/styles/heroui)       | 每个文件适配一个组件，消费共享 token，不另建主题         |

HeroUI 默认值位于 CSS `base` layer，项目全局 token 与主题覆盖也使用该层并遵守导入顺序。依据实际组件 class、状态属性和样式源码适配，不猜测 `data-slot`。

颜色使用 `background`、`foreground`、`surface`、`overlay`、`muted`、`border`、`separator`、`field-*`、`accent`、`focus`、`success`、`warning`、`danger` 及对应 foreground token。已有 `material-*` 仅用于确有材质需求的局部容器，不要求所有导航或内容增加模糊。不要在页面硬编码亮暗色值或复制组件 token。

外观与配色独立：`data-theme="light|dark"` 和 `.dark` 表示解析后的外观，`data-palette` 表示配色。系统外观、持久化和配色清单由 [theme.ts](apps/web/src/lib/theme.ts) 管理。默认浅色背景为白色、暗色背景为纯黑；强调色以主题文件为准，不另维护色值副本。

### 字体、尺寸与状态

正文使用 `-apple-system, BlinkMacSystemFont, "SF Pro Text", "Segoe UI", sans-serif`；代码使用 `"SFMono-Regular", ui-monospace, Menlo, Consolas, monospace`。字体名仅作为本机候选，缺失时回退，不下载专有字体或远程字体。中文与其他文字使用可用系统字形。

| 项目       | 基准                                                                                   |
| ---------- | -------------------------------------------------------------------------------------- |
| 正文基准   | 17px；字段和辅助信息按层级使用较小字号                                                 |
| 标题 token | `text-large-title` 34px、`text-title-1` 28px、`text-title-2` 22px、`text-title-3` 20px |
| 默认圆角   | `--radius: 0.75rem`（12px）                                                            |
| 字段圆角   | `--field-radius: 0.875rem`（14px）                                                     |
| 材质圆角   | `--radius-material: 1.25rem`（20px），按实际容器使用                                   |
| 默认过渡   | 180ms，使用全局缓动                                                                    |
| 键盘焦点   | 2px 轮廓、2px 偏移，颜色来自 `focus`                                                   |

不强制所有页面标题使用同一字号，也不为每种列表或浮层再规定独立圆角。保留 HeroUI 与共享组件已有层级。

主要触控操作目标至少 44×44px，文字按钮允许更宽。普通文字对比度目标至少 4.5:1；焦点和必要的控件边界至少 3:1。错误、选中、禁用和处理中不能只用颜色区分。新增或调整配色时实际检查对比度。

## 应用框架

同一转换工作区的输入编辑器与代码结果共用字段背景、边框、圆角、工具栏底色和标题层级；高亮主题只提供语法颜色，编辑器画布与行号区保持透明，避免覆盖应用主题。参数跟随左侧输入，以分隔线和字段间距组织，不额外套一张不同底色的卡片。

工具工作区采用单层内容组件：标题与必要说明在顶部左侧，复制、下载、全屏和取消等相关操作集中在同一工具栏右侧。窄屏工具栏可换行，主要操作始终可见。代码结果直接填充内容区，不再叠加外层 Panel、重复标题和独立底部下载栏；加载、空结果与错误也在同一内容区呈现。输入侧遵循相同层级，保留任务确实需要的文件拖放入口。文本、图片、文件列表与媒体采用各自的内容组件，共享布局规则，不建立万能业务组件。按工具家族应用这些规则。

转换工具的输入、文件入口和选项属于同一个工作区字段外壳；选项跟随输入侧组织，窄屏保持可读的两列上限。编辑器和结果组件各自拥有标题与说明，结果工具栏同时承载复制、下载、取消等操作。可下载成功态使用与禁用态相同尺寸和语义变体的官方按钮样式，避免状态切换造成布局跳动。加载骨架由结果内容区承载，不在已有结果外壳中再套一层边框、圆角或内边距。

操作靠近作用对象：示例、导入和粘贴等输入准备操作放在输入区左上、标题附近；清空、重置和全屏放在输入工具栏右侧；复制、下载与导出放在结果工具栏右侧。不要为一个示例或辅助按钮单独增加底栏。主要执行按钮按任务顺序放在参数之后或工作区工具栏；只有持续操作或必要状态展示才保留底栏。

Header、页面内容与 Footer 共用 `.page-container`：最大宽度 80rem，水平内边距默认 1.25rem、40rem 起为 2rem、64rem 起为 2.5rem。正文可限制阅读宽度，但主标题和工作区保持共同内容轴，不再增加 PageContainer 包装组件。

站点框架由 [语言布局路由](apps/web/src/routes/{-$locale}/route.tsx) 统一提供。Header 是开放的单层导航，包含品牌、工具、语言、配色与外观入口；API/MCP 入口仅在提供对应功能的分支展示；当前入口有明确选中态。窄屏允许隐藏品牌文字和压缩辅助标签，核心操作不能溢出。不把吸附或玻璃导航作为默认要求。

首页采用标题、搜索与浏览入口、能力说明、分类及热门工具的顺序。分类和工具优先用图标、文本、留白与必要行分隔组织，数量从 catalog 推导。

首页的能力条、分类行和热门工具行需要保留足够的上下留白、横向内边距和图标与文字间距；标题、名称与说明按层级分隔，避免内容贴边或挤在一起。示例或快捷入口靠近对应输入标题与操作，不单独堆在页面底部形成无关页脚。

工具目录桌面采用左侧分类、右侧搜索与结果；窄屏分类横向滚动。搜索、分类、用途筛选、清空和空结果状态保持一致；URL 中的状态支持刷新、分享和前进后退恢复。

## 工具详情页

各工具页面内部按需组合 [工具页布局](apps/web/src/features/tools/_shared/tool-page/page.tsx)，提供：名称、简短说明、必要的联网提示、工作区与使用说明。不要强制添加每页都没有的面包屑、图标、状态徽章或相关工具区。

有独立输入与结果且需要对照的工具，桌面优先双栏、窄屏上下排列；简单即时反馈可上下排列，单一查询、列表或画布不制造空结果栏。复用已有 Panel 和布局，不依赖并不存在的全局布局 class。工作区避免再套一层装饰背景或重复边框。

主要区块通常使用 `gap-8 sm:gap-10`，同级 Panel 使用 `gap-6`，字段组使用 `gap-4`，标签与控件及并列操作使用 `gap-2`。标准工具 Panel 复用 [tool-panel-card.tsx](apps/web/src/components/base/tool-panel-card.tsx) 的间距和操作区。媒体画布、计时显示、代码区与密集表格可局部调整内边距，不改变同级工作区的外部节奏。

分隔线只表达语义边界。开放区块优先留白，不同时叠加外框、上下线、列线和逐行线。长结果在自身容器内滚动，避免整页双轴滚动。

### 数据表格

表格由一个明确的 surface 负责承载。外层已有 `ToolPanelCard` 时，优先使用 HeroUI `Table` 的 `secondary` variant，让表格保留标题 token、列分隔、行分隔和 cell padding，而不再叠加背景、阴影或圆角；独立表格才使用带内层 surface 的默认 variant。`Table.ScrollContainer` 只负责局部横向或纵向滚动，宽表不推动页面产生横向滚动。

表头使用 `surface-secondary` 与 `muted` 层级，行和 cell 使用 `separator` token 表达边界，悬停和选中状态由 HeroUI 样式提供。页面不要为表格硬编码亮暗色、重复圆角或额外套一张边框容器；原生 HTML 表格也遵循相同的 surface、padding、分隔线和滚动规则。文档排版中的 `table-row`/`table-cell` 不属于数据表格规则。

能力不支持时给出明确原因；运行中、成功、失败、取消与重置都应有可理解的反馈。兼容性信息直接展示，高级选项才使用 Accordion 或 Disclosure。开发者接入信息只在提供该功能的分支集中展示。

## 表单、文件和代码

字段提供可见 Label，使用 Description 与 FieldError 关联说明和错误。长列表或可搜索选项使用 Autocomplete，短固定选项使用 Select，布尔设置使用 Switch，独立多选使用 Checkbox。组合选项包含可访问的纯文本名称，按 HeroUI 的复合组件结构实现。

文件入口复用 [tool-file-picker.tsx](apps/web/src/components/base/tool-file-picker.tsx)，支持所需的点击、键盘与拖放。显示格式和大小限制；多文件使用一致的名称、大小和移除列表。选择、更换、清空、复制和下载必须是明确的可访问操作。颜色输入同时展示色样和文本值。

代码输入与输出复用 [code-editor.tsx](apps/web/src/components/base/code-editor.tsx) 和 [code-block.tsx](apps/web/src/components/base/code-block.tsx)，共用本地 Monaco/Shiki 语言与亮暗主题。技术内容保持 LTR；自然语言和普通结果允许换行，只有代码或宽表格在局部横向滚动。运行时不通过 CDN 加载编辑器、高亮器和主题。

路由骨架必须镜像实际工作区的列断点、字段外壳、工具栏、输入高度和选项归属；删除底部操作栏或独立选项区时，同步从骨架移除对应占位。HeroUI `Switch.Content` 应包含 `Switch.Control` 和 `Switch.Indicator`，让文字与控件共享完整的可点击目标。

## 用户任务与交互

核心流程是“找到工具 → 理解要求 → 输入 → 执行 → 理解结果 → 复制或下载”。减少完成任务所需的判断和重复操作，同时让数据流向、执行状态和恢复方式明确。下列要求按工具能力适用，不为不存在的需求增加步骤。

| 环节 | 交互要求                                                                                       |
| ---- | ---------------------------------------------------------------------------------------------- |
| 发现 | 搜索覆盖工具名称、常用别名和任务描述；筛选状态可见，无结果时提供清空筛选或相关入口             |
| 理解 | 简述用途、输入格式和限制；复杂工具提供可运行的非敏感示例，示例替换现有输入时让用户明确知道影响 |
| 输入 | 提供合理默认值和可见单位；校验尽量靠近字段，在适当时机触发，避免用户尚未输入完就持续报错       |
| 执行 | 主操作名称表达具体动作；耗时操作防止重复提交，并提供可实现的取消或停止入口                     |
| 结果 | 主结果优先，次要信息其次；区分尚未执行、处理中、有效空结果、成功和失败                         |
| 使用 | 复制与下载靠近对应结果；文件名、扩展名和内容格式一致，操作成功后才显示成功反馈                 |
| 恢复 | 错误说明位置、原因和下一步；失败或重试保留可复用输入，不让用户从头开始                         |

### 按工具类型选择流程

| 类型           | 默认交互                                                                             |
| -------------- | ------------------------------------------------------------------------------------ |
| 即时转换       | 轻量计算随输入更新；昂贵计算采用合适的延迟或手动执行，输入过程保持流畅               |
| 文件处理       | 选择、校验、设置、处理、预览与下载；格式不支持时在昂贵处理前提示                     |
| 生成           | 默认设置可直接执行；重新生成明确替换哪个结果，不因无关渲染改变结果                   |
| 联网查询       | 明确查询动作、目标服务与请求状态；保留条件并提供重试，不在输入每个字符时无条件发请求 |
| 列表与参考     | 搜索与筛选优先，结果可扫描；详情按需展开，不强加执行按钮或空输出区                   |
| 媒体画布与录制 | 预览为核心，工具栏就近；权限请求解释用途，停止、导出和资源释放清晰可达               |

### 状态、取消与重置

输入或参数变更后，即时结果同步更新；手动执行工具将旧结果标记为待更新或清除，不能把旧结果显示为当前输入的产物。并发任务只接受仍然有效的结果，取消、重置或更换输入后，旧任务不能回写覆盖新状态。

耗时操作及时提供文字状态，有真实可测进度才展示百分比，不伪造完成时间。取消后保留可复用输入；批处理明确哪些成功、失败或未处理，并允许使用已成功的结果。

“清空输入”“恢复默认设置”“清除结果”使用准确名称；一个重置操作涉及多项内容时说明范围。普通可逆操作不额外增加确认弹窗；可能丢失难以恢复的编辑时，优先提供撤销或必要的确认。

复制失败、下载生成失败与浏览器权限拒绝分别反馈，不提前宣告操作成功。提示靠近对应操作，避免通过跳动布局或每次弹窗打断连续使用。

### 连续使用与无障碍

目录搜索与筛选使用 URL 恢复刷新、分享和前进后退状态；用户文件、密钥及敏感文本不进入 URL。主题、语言等偏好可持久化，工具输入与结果不默认持久化。离线状态下准确区分仍可用的本地能力与需要联网的操作。

键盘可完成核心任务；图标按钮有可访问名称，弹层关闭后焦点返回触发位置，错误字段可定位。异步状态使用合适的可访问通知，避免即时转换每次按键都触发读屏播报。手机不依赖 hover 暴露关键操作，文本缩放后功能仍可达。

## 开发者页面（功能分支）

本节只适用于 `feature/api-mcp`。`main` 不展示开发者接入导航、页面或调用示例；网页工具的用途和操作说明仍需保留。

`/api` 从运行时 OpenAPI 生成内容，保留搜索、筛选与下载。端点显示方法、路径和说明；Disclosure 默认收起，展开后再挂载较重的请求与响应代码内容。加载失败、无结果和重试有明确反馈。

`/mcp` 集中提供全局连接方式与可搜索的分类工具清单。普通工具目录使用本地化名称和说明；协议示例需要时可以展示准确的 operation 标识，不用内部标识替代产品名称。

法律页面与 MCP 页面遵循站点共同的标题字号、内容轴和间距层级。长文内容使用连贯的可读正文区和响应式布局，避免窄列贴左并留下大块空白，也不把每段文字拆成装饰卡片。

## 响应式、语言和隐私

桌面、平板与 390px 页面不得整体横向滚动；同时考虑全局 320px 最小宽度。检查长中文、英文、文件名和错误文本。布局优先使用逻辑方向属性；未来增加 RTL 语言时验证方向与技术输出。

文案和 SEO 来自 Paraglide；语言以 [settings.json](apps/web/project.inlang/settings.json) 为准。默认中文无前缀，非默认语言使用 BCP 47 前缀，切换语言保留路径、查询参数和 hash。

“本地处理”必须符合实际执行位置。浏览器文件、名称、元数据与结果不上传、不进入日志或离线缓存。联网工具说明目标服务；功能分支中的 HTTP API 与 MCP 在部署环境处理，不能描述为调用者本机处理。

交互采用短暂的颜色、透明度和位置过渡，避免 bounce、大幅 zoom 与装饰动画。`prefers-reduced-motion` 下移除非必要动画和平滑滚动，保持状态反馈可见。

## 评审清单

- 复用 HeroUI 与已有公共组件，颜色来自语义 token。
- 共同内容轴、工作区间距与亮暗模式一致，配色变化不改变布局。
- 标签、键盘顺序、焦点、触控尺寸、错误与加载状态完整。
- 桌面、平板及 390px 无页面级横向溢出，长文本与窄屏操作可用。
- 从首次访问到结果使用的完整任务可完成；默认值、示例、限制和主操作清楚。
- 搜索与 URL 恢复一致，输入改变后没有误导性的旧结果，取消后没有过期任务回写。
- 失败和重试保留输入，重置范围明确，复制与下载反馈准确。
- 键盘、读屏状态通知、文本缩放和手机操作覆盖核心流程。
- 文件处理位置与联网说明准确，用户内容不进入缓存或日志。
- 可见修改在用户已启动的真实可见浏览器中验证；静态检查不等于视觉验收。
